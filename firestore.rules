rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /employees/{empId} { allow read, write: if true; }
    match /teams/{teamId} { allow read, write: if true; }
    match /attendance/{attId} { allow read, write: if true; }
  }
}